# Live Counter

The prose you are reading is static server HTML; the component below is a genuine
GoSX island compiled to bytecode and hydrated in your browser.

Click the buttons — the count is real reactive state, and editing `Counter.gsx`
with `serve --watch` hot-swaps the running island without losing it.

<Counter/>
